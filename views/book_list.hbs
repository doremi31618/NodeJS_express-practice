

{{#if err}}
    <p>something went wrong</p>
    err
{{else}}
    
    <ul>
        {{#each book_list}}
        <li><a href="{{this.url}}">{{this.title}} | {{this.author.name}}</a></li>
        {{else}}
        <li>no content</li>
    </ul>
    {{/each}}
{{/if}}